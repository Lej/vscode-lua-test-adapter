{
  "version": "1.0.0",
  "configurations": [
    {
      "type": "extensionHost",
      "request": "launch",
      "name": "Lua Test Adapter",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": [
        "${workspaceFolder}/out"
      ]
    }
  ]
}
